<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jbs.formulawithfun">



<!-- 🎙 Required permissions for voice note recording -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- ✅ Core screens -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".StandardSelectionActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".OptionsActivity" />
        <activity android:name=".MarkedFormulasActivity" />
        <activity android:name=".SpecificFormulaActivity" />


        <!-- Add other required activities below if needed -->

    </application>
</manifest>
